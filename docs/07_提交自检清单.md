# 提交自检清单（P2-11）

> 检查日期：2026-09-11 · 作品仓 HEAD：见 `git log -1`
> 勾选依据：**真机实测** 或 **可复现命令验证**，逐项附证据。

## 一、仓库与交付形态

- [x] **专属仓可公开访问**：`contest2026_342_ezdezhandui`（本仓）
- [x] **目录结构符合提交指南**：`board/esp32p4_vela/`（作品代码）+ `docs/` + `logs/` + `tools/`
- [x] **README 含作品名称/赛道/运行方式/简介**：根 `README.md` §一~§四
- [x] **运行方式可复现（四步）**：`repo init → sync → deploy → build` → **两镜像烧录** → console
  - 证据：干净构建目录 `/tmp/repro_build` 从零构建 **4:20 成功**，产物烧录后正常启动
- [x] **manifest 完整性**：**324 条 `<copyfile>`**，源文件 0 缺失、dest 0 重复、XML 解析通过
- [x] **revision 已锁 SHA**：`board/esp32p4_vela/lock-revision.sh` 执行，锁定到含文件树的提交
- [x] **deploy.sh 幂等**：执行后 `nuttx/`、`apps/` 工作区零残留

## 二、赛题四点要求

- [x] **① Agent 在硬件上跑起来**
  - 移植：console / eth0 / 32MB PSRAM / **MCUboot + flash XIP** 全部真机通过
  - ai_agent：P0→P6 全 rc=0，36 tools / 12 skills
  - 证据：`docs/demo/01_boot_mcuboot.log`、`docs/demo/02_verify.log`
  - ⚠️ 限制：LLM 真实对话受 MiMo key（HTTP 401）阻塞；串口 CLI 输入未通
- [x] **② ≥1 个自定义 Skill**
  - 交付：`中控助手`(center-assistant) + `速记工单`(quick-note)
  - 证据：真机 `Skills system ready (12 built-in)` + `docs/demo/` 日志
- [x] **③ ≥1 个「主动+执行」场景**
  - 定时主动：cron 服务真实运行（`[cron] Cron started`）
  - 事件主动：MIPI-CSI/SC2336 已识别并注册 `/dev/video0`（`chip ID 0xcb3a`）
  - ⚠️ 限制：摄像头出帧待 ISP 打通；cron 需补"到点执行"演示
- [x] **④ 交付完整应用场景说明**：`docs/06_作品介绍.md`（用户故事/功能清单/技术实现/证据/限制）

## 三、评分项材料

- [x] **技术难度**：MCUboot 移植（上游无 p4 端口）+ flash XIP + PSRAM 攻坚 + CSI/DSI 驱动，
      全过程证据链在 `docs/05_开发过程复盘与改进清单.md`
- [x] **项目完整度**：四点要求材料齐备；`docs/01`（规划+差距+计划）、`docs/02`（需求风险）、
      `docs/03`（模块设计）、`docs/04`（闭环测试）、`docs/05`（复盘）、`docs/06`（作品介绍）、
      `docs/踩坑笔记_01~03`、`docs/demo/`（真机证据）
- [x] **AI 开发**：`logs/ez-xu/` **18 会话 / 11,074 事件**，`validate-log.py` 校验 **ALL OK**
- [x] **沉淀**：openvela 官方 17 个技能（`.claude/skills/`）+ 本仓文档体系
- [ ] **演示视频（≤5 min）**：⏳ **待录制** —— 素材已齐（见 `docs/demo/`），脚本见下

## 四、演示视频脚本（≤5 分钟，可直接照录）

| 段落 | 时长 | 内容 | 依据 |
|---|---|---|---|
| 1. 开场 | 20s | 作品一句话 + 硬件实物（P4X-C5 板 + 屏 + 摄像头） | README §一 |
| 2. 移植硬货 | 60s | 执行 `tools/usb_stable.sh` → MCUboot banner → `Mapped IROM` → `nsh>`；讲"512KB→释放 430KB" | `docs/demo/01` |
| 3. 环境能力 | 40s | `free`（PSRAM 33.9MB）、`ifconfig eth0`（10.0.0.2）、`ls /dev`（video0/input0） | `docs/demo/03` |
| 4. 自定义 Skill | 50s | `ai_agent` 启动 → `Skills system ready (12 built-in)` → 演示工单/中控技能 | `docs/demo/02` |
| 5. 主动场景 | 60s | cron 到点触发（录屏等到点）；摄像头 `chip ID 0xcb3a` 识别 | `docs/demo/02` |
| 6. 收尾 | 30s | 诚实说明：屏/触摸/采集/LLM 的现状与下一步 | `docs/06` §六 |

> ⚠️ 录制前请先确认 LCD 模组连接（FPC 方向 + J1→J6 跳线），屏点亮后再录第 6 段效果最佳。

## 五、提交前最后检查

- [x] 无密钥泄露：`agent_secrets.h` 在 `.gitignore` 中；`.gitignore` 已补构建产物与临时备份规则
- [x] 无构建产物入库：已清理 `build-esp32p4-bootloader/`、`*.pre-*-backup`
- [x] `.deleted-files` 与删除清单一致（8 个上游删除文件）
- [ ] **push 到 fork 并提 PR**（未执行 —— 属主动操作，需人工确认）

## 六、未完成项（诚实记录，不阻塞提交）

| 项 | 状态 | 说明 |
|---|---|---|
| LCD 屏点亮 | 🔶 硬件层 | 面板 DCS 无应答 + GT911 静默；I2C0 本身可用 → 指向模组连接/供电 |
| 摄像头出帧 | 🔶 需 ISP | 模块识别成功；RAW10 与 V4L2 上层语义不匹配 |
| ai_agent 串口输入 | 🔶 待排查 | 输出侧已修复；输入侧 NSH 与 agent CLI 均收不到字符 |
| LLM 真实对话 | 🔶 外部 | MiMo key HTTP 401，需有效额度 |
| 演示视频 | ⏳ | 素材已备，脚本已给出 |
| 语音（ES8311） | 📝 未做 | 驱动未落地 |
