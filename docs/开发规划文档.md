# 桌伴 · ESP32-P4X 智能视觉中控屏 —— openvela 大赛项目方案 V5.1

**版本**：V5.1（2026-08-12，平台由匠芯创 D12X 变更为 **ESP32-P4X-Function-EV-Board**（用户选定）；按大赛官方赛题格式组织）
**赛道**：**双线并打**——① 新硬件平台适配（ESP32-P4X 为官方「待适配」板，重点加分方向）② AI 硬件产品创新（基于 openvela + ai_agent 的「能主动、会执行」应用）
**代码基线**：openvela 大赛分支 `dev-ai-contest-2026`；**提交**：专属仓 `contest2026_<编号>_<队伍名>` → fork + PR 自行合入；获奖后 PR 至 `vendor_espressif` 大赛分支

## 〇、为什么是 ESP32-P4X（平台选择依据，官方文档核实）

| 维度 | 事实 |
|---|---|
| 官方身份 | **官方「待适配开发板」**（supported_hardware.md 明确列出，提供芯片手册/硬件文档/参考代码，适配=重点加分方向） |
| 适配落点 | **上游目标仓 `vendor_espressif`（dev-ai-contest-2026）已存在**（boards/ 已有 esp32s3 先例）——PR 有明确去处 |
| 移植参考 | **NuttX 上游 esp32p4 全套支持**（arch/risc-v/src/esp32p4 + boards/risc-v/esp32p4/esp32-p4-function-ev-board + common）——非从零发明 |
| 硬件能力 | 双核 RISC-V 400MHz + **32MB PSRAM** + **RJ45 以太网（联网保底）** + **板载麦克风/扬声器（ES8311 codec + NS4150B 功放）** + MIPI-CSI 2MP 摄像头 + MIPI-DSI 7" 1024×600 触摸屏 + WiFi6/BLE5（C6 协处理器）+ H264 编码 + USB/SD |
| 资料 | 官方 esp-dev-kits user_guide 完整（接线/外设/示例） |
| 申领 | 官方板，走申领流程（方向匹配审核 + 50 押金） |
| 对比 LogicPi | LogicPi 的 `vendor_amlogic` **仓库/大赛分支待创建**（适配 PR 无落点）；P4X 目标仓已建 + 上游参考全套 |

## 一、项目概述

**作品名称**：桌伴（DeskMate）—— 基于 openvela + ai_agent 的 ESP32-P4X 智能视觉中控屏
**一句话**：一块 7 英寸的 AI 视觉中控屏——它「能看见、会主动、会执行」：摄像头看到你进门就主动问候，早上 8 点自动播报天气和日程，说"记一下明早 9 点开会"就记下并到点提醒，家里设备异常它主动告警。全程离线也能响应本地指令，联网后 LLM 加持。
**运行方式**：编译 `vendor_espressif/boards/esp32p4/esp32-p4x-function-ev-board/configs/<config>/` → 烧录 → `ai_agent` → `router_set mimo <key>` → 对话/主动任务。

## 二、赛题四点要求对齐（官方 ai_hardware_track_guide）

| 官方要求 | 本项目交付 |
|---|---|
| **① Agent 在硬件设备上跑起来** | ESP32-P4X 移植 openvela + ai_agent 固件烧录运行；MiMo LLM（大赛 Token Plan）；交互渠道：CLI + LVGL 触控 + **语音（板载麦克风 ES8311 + 扬声器，火山 ASR/TTS）** |
| **② 至少 1 个自定义 Skill** | `中控助手`（智能家居/中控场景技能集）+ `速记工单`（语音/文字→结构化记录）——Markdown Skill 于 `/data/agent/skills/`，附使用演示 |
| **③ 至少 1 个「主动+执行」场景** | **事件主动（核心）**：MIPI-CSI 摄像头检测到人/异常 → Agent 主动问候/告警（屏显+TTS）；**定时主动**：每日 8:00 天气+日程简报；**上下文主动（进阶）**：记忆机制（偏好/习惯建议） |
| **④ 交付完整应用场景说明** | 用户故事 + 功能清单 + 技术实现（本文件 §三，另出《作品介绍》随提交） |

## 三、产品定义

### 3.1 用户故事

> 独居的上班族小林回家：门口的「桌伴」摄像头认出他，屏幕亮起"欢迎回家，今天 24℃，晚间有雨"。他说"帮我记一下明早 9 点项目评审"——桌伴记下并到点提醒。深夜摄像头检测到阳台异常人影，桌伴立即屏显+TTS 告警并推送到手机。他全程动嘴不动手。

### 3.2 功能清单（V1 必达）

| 功能 | 实现 | 对应赛题点 |
|---|---|---|
| openvela 移植 ESP32-P4X | 板级+芯片移植（NuttX 上游参考）→ 串口/ETH/屏/触摸/摄像头/codec 驱动 | 适配赛道核心 |
| LLM 对话 | ai_agent + MiMo（`router_set mimo`）；**联网=以太网保底/WiFi 待验证** | ① |
| LVGL 触控 UI | 7" 屏：对话卡片 + 简报卡片 + 摄像头画面 + 设备状态 | ① + 加分（智能家居/设备互联） |
| 语音交互 | 板载 MIC（ES8311）→ 火山 ASR；TTS 播报（扬声器） | ①（唤醒词用官方指定"你好，openvela"） |
| 自定义 Skill ×2 | 中控助手、速记工单 | ② |
| 事件主动（摄像头） | MIPI-CSI 2MP → 本地视觉（人形/人脸检测，CPU 轻量模型）→ 主动问候/告警 | ③（核心区分点） |
| 定时主动 | cron_add + weather/daily-briefing + TTS | ③ |
| 记忆机制（进阶） | ai_agent memory_read/write（偏好/历史） | 加分 |
| 端云协作（进阶） | 设备端 Agent ↔ 电脑端（任务拆分演示） | 加分 |
| H264 编码（加分） | P4 硬件编码（摄像头流媒体/可视门铃叙事） | 多媒体能力落地 |

### 3.3 技术实现要点

- **移植路径（技术难度主攻）**：NuttX 上游 `arch/risc-v/src/esp32p4` + `boards/risc-v/esp32p4/esp32-p4-function-ev-board` → openvela（`vendor_espressif/boards/esp32p4/` 板级 + nuttx 侧同步）；参考同仓 esp32s3 的 vendor 化模式；外设按「串口→ETH→屏/触摸→摄像头→codec」渐进
- **P4X 与 P4 差异**：P4X-Function-EV-Board = ESP32-P4 芯片 rev v3.1+ 变体，NuttX 上游板目录（esp32-p4-function-ev-board）为移植基线，P4X 差异点（芯片 rev/个别引脚）移植时核对 user_guide 修正
- **ai_agent 集成**：参照官方 esp32s3-eye 模式（`defconfigs/<板>/` + `fix_<板>.sh` 处理 mbedtls 冲突/兼容性问题）——esp32s3 先例经验直接复用（同 vendor 仓、同工具链生态）
- **联网**：**以太网（RMII 10/100）为保底**（ai_agent/LLM 依赖）；ESP32-C6 WiFi6 为无线演示增强（NuttX esp32p4 WiFi 支持状态待验证）
- **摄像头**：MIPI-CSI 2MP；NuttX 上游 esp32p4 有 camera 驱动参考；本地视觉用轻量模型（CPU），避免 NPU 依赖
- **内存**：32MB PSRAM 充裕（ai_agent 最小 256KB + LVGL + 视觉缓存）
- **「基于 openvela」判定对齐**：图形（LVGL）+ AI（ai_agent）+ 多媒体（TTS/摄像头/H264）全落地

## 四、评分对照（官方：技术难度 30/创新 20/完整度 20/AI 10/商业 10/展示 10）

| 评分项 | 本项目打法 |
|---|---|
| **技术难度 30** | ① ESP32-P4X 板级移植（RISC-V 新平台全套外设）② ai_agent × P4X 集成（首个）③ 摄像头视觉 + H264 深度使用 openvela 能力 |
| **产品创新性 20** | "能看见、会主动"的**家庭视觉中控屏**（可视门铃/看护/中控三合一），摄像头事件主动是赛题「事件主动」的最佳载体，区别于被动问答 |
| **项目完整度 20** | 四点要求全交付 + 演示视频（≤5 分钟稳定 3 遍）+ 介绍文档 + logs/ + 代码可审计 |
| **AI 开发 10** | 全流程 AI Coding + 日志导出 + 沉淀 Skill（AI 编码 skill + ai_agent skill 双沉淀） |
| **商业潜力 10** | 智能家居中控屏/可视门铃是真实市场（P4X 官方定位场景）；小米生态叙事 |
| **展示效果 10** | 答辩演示：摄像头事件主动（进门问候）→ 语音/触控 → Agent 执行 → 屏显+TTS 反馈 |

## 五、里程碑（2026-08-12 起，39 天；移植为主线）

| 阶段 | 时间 | 主线（移植 + ai_agent） | 平行任务 |
|---|---|---|---|
| T1 | 8/12–8/20 | ① 申领 ESP32-P4X（方向匹配审核→50 押金）② **openvela 树切 dev-ai-contest-2026** ③ **esp32p4 移植：串口 console 点亮**（上游代码合入 + U-Boot/bootloader 分析） | MiMo 实名+Token；GitHub 协作邀请；esp-dev-kits user_guide 通读 |
| T2 | 8/21–9/5 | ④ 外设驱动：ETH→屏/触摸→codec（音频）→摄像头 ⑤ **ai_agent 集成**（defconfig + fix 脚本 + MiMo 对话跑通） | 板卡到货联调 |
| T3 | 9/6–9/15 | ⑥ LVGL 中控应用 + 自定义 Skill ×2 + 事件主动（摄像头）+ 定时主动 ⑦ **9/10 演示冻结**（视频 ≤5 分钟）⑧ 提交材料（介绍文档+视频+README+logs/） | P4X 移植 PR 整理 |
| T4 | 9/16–9/20 | **9/18 前提交**（自检清单逐项勾验） | — |
| T5 | 10 月 | 决赛答辩：移植技术点 + 主动场景演示 + Q&A | — |

## 六、仓库结构（按官方提交指南）

```
contest2026_<编号>_<队伍名>/       # GitHub 专属仓（public，linkfile 映射）
├── esp32p4_vela/                  # 作品代码（子目录）
│   ├── boards/esp32p4/            # 板级（移植主体，映射 vendor_espressif）
│   ├── ai_agent_defconfig/        # ai_agent × P4X（defconfig + fix 脚本）
│   ├── skills/                    # 自定义 Skill
│   ├── lvgl_app/                  # LVGL 中控应用
│   └── README.md
├── logs/                          # AI Coding 日志（主动导出）
└── README.md                      # 作品名称/赛道/运行方式/简介
```

## 七、风险登记册

| # | 风险 | 等级 | 应对 |
|---|---|---|---|
| R1 | **移植工作量与版本差异**（openvela nuttx fork 落后于 NuttX 上游 esp32p4 代码） | 🔴 | T1 首周即移植串口试错（上游全套参考 + esp32s3 同仓先例）；9/5 未完成 → goldfish 全功能演示 + P4X 静态移植材料兜底 |
| R2 | 板卡到货延迟（审核 3 天 + 发货 3 天） | 🟡 | 立即申领；移植开发不依赖真机（代码先行）；goldfish 并行验证 ai_agent 功能 |
| R3 | WiFi（C6 协处理器）在 NuttX/esp32p4 支持待验证 | 🟡 | **以太网 RMII 保底联网**（ai_agent/LLM 不受阻）；WiFi 作为真机增强 |
| R4 | ai_agent × RISC-V P4 编译兼容问题（mbedtls 等） | 🟡 | esp32s3-eye 的 fix_esp32s3.sh 经验直接迁移（同 vendor 仓）；官方已趟过的坑清单在手 |
| R5 | 摄像头视觉性能（CPU 轻量模型） | 🟡 | 人形/人脸检测轻量模型；V1 只做「检测到人→主动问候」最小闭环 |
| R6 | 时间压缩（移植+集成+应用 39 天） | 🔴 | 外设按优先级渐进（ETH/屏/audio 必达，摄像头/H264 后置）；T3 冻结不可移 |
| R7 | **7" 屏与 2MP 摄像头为选配**（MIPI 连接器"可配"）——申领配套不确定 | 🟡 | 申领时确认配套；不随板 → V1 演示用板载能力（LCD 无则 CLI/串口/语音闭环）+ 摄像头事件主动降为加分；esp-dev-kits 示例（LVGL/摄像头）可先于真机在模拟器/PC 侧验证 |

## 八、待办清单（T1 立即执行）

- [ ] **申领 ESP32-P4X-Function-EV-Board**（报名审核状态 → 物资登记表 → 方向匹配审核 → 50 押金 → 顺丰）＋ 确认配套（7" 屏 + 2MP 摄像头模组是否随板/选配）
- [ ] **MiMo 实名认证** + Token Plan；GitHub 接受专属仓协作邀请（7 天有效期）
- [ ] **openvela 树切 dev-ai-contest-2026**（组委会 manifest 未到先用公开源）→ goldfish 重构建回归
- [ ] NuttX 上游 esp32p4 代码评估（arch + boards 依赖树、与 openvela fork 的 diff 量）
- [ ] esp32p4 移植首步：串口 console（上游代码合入 openvela nuttx + vendor_espressif 板级骨架）
- [ ] 通读 esp-dev-kits user_guide（接线/外设/示例）

## 附：参考依据（官方文档）

- 官方硬件清单（P4X 为待适配板 + 目标仓 vendor_espressif）：`docs` 仓 dev-ai-contest-2026 `zh-cn/contest_2026/hardware_porting/supported_hardware.md`
- 赛题四点要求：`ai_hardware/ai_hardware_track_guide.md`；ai_agent 上手：`ai_agent_quickstart.md`（esp32s3-eye/gemini-s1/goldfish 预置，P4X 需自建）
- 提交指南：`code_submission_guide.md`；评分规则：`contest_overview.md`（30/20/20/10/10/10）
- NuttX 上游 esp32p4：apache/nuttx `arch/risc-v/src/esp32p4` + `boards/risc-v/esp32p4/esp32-p4-function-ev-board`（API 核验 2026-08-12）
- P4X 板卡：espressif/esp-dev-kits user_guide（RJ45/ES8311 MIC+SPK/NS4150B/MIPI-CSI-DSI/C6 模块/32MB PSRAM）

---

*V5.1 定稿 2026-08-12（平台按用户选定改为 ESP32-P4X）。历史：V5.0（匠芯创 D12X，8/12）、V4.x（LogicPi，废弃）、V3.x（站灯开灯）、V2.x（RK3576）、V1.x（A311 初版）。*
