# 开发过程复盘与改进清单（2026-08-06 → 09-04）

> 本文档沉淀项目至今的完整开发过程与踩坑教训，以及后续改进清单，确保不走弯路。
> 配套：踩坑笔记 #01（环境+模拟器）/ #02（移植）/ #03（烧录与启动）（单点细节）、开发规划文档.md（方案 V5.1）。
> **当前主线状态（9/4）**：PSRAM 32MB heap 已打通（PMP 修复）→ ai_agent 已完整启动并稳定运行。详见 §10.8。

## 一、项目全景

- **定位**：openvela AI 硬件开发者大赛（小米）· 桌伴 DeskMate —— 7 英寸"能看见、会主动、会执行"的 AI 视觉中控屏
- **赛道**：双线并打——① 新硬件平台适配（ESP32-P4X 官方待适配板）② AI 硬件产品创新（ai_agent 主动场景）
- **评分**：技术难度 30 / 创新 20 / 完整度 20 / AI 开发 10 / 商业 10 / 展示 10；**9/10 演示冻结、9/18 提交、9/20 截止**
- **平台选择历程**：A311（V1）→ RK3576（V2，无板）→ 站灯（V3）→ LogicPi A1（V4，弃：vendor_amlogic 仓未建、无上游参考）→ 匠芯创 D12X（V5.0，弃：官方已适配、8MB 内存弱）→ **ESP32-P4X（V5.1 定稿）**：官方待适配板 + vendor_espressif 目标仓已建（esp32s3 先例）+ NuttX 上游全套参考 + 硬件强（32MB PSRAM / RJ45 保底联网 / 板载 MIC+SPK / MIPI-CSI/DSI / C6 WiFi6 / H264）

## 二、开发时间线（8/6–8/20）

### 阶段 0：环境搭建（8/6，踩坑笔记 #01）——goldfish 模拟器跑通
repo 2.17 太老 → 换新版；gerrit 自举被墙 → ghfast 代理；tuna 挂死 → gitee 源；python2 抢 CMake → PATH 前置；缓存残留 → rm cmake_out；模拟器缺镜像 → 软链产物；无显示器 → -nographic。

### 阶段 1：方案定稿 + ai_agent 验证（8/12，踩坑笔记 #01）——goldfish + ai_agent 跑通
- ✅ `ask 现在几点了` 内置工具应答（无 LLM key 也通，ReAct 链路工作）
- 交互通道血泪史：**stdin 管道 + -qemu -nographic 是唯一可靠通道**（expect/pty/adb/telnet 全不通）
- 教训：`pkill -f` 三次误杀自己（用 `[q]emu` 字符类）；多模拟器 AVD 互斥；cwd 漂移

### 阶段 2：esp32p4 移植（8/12–18，踩坑笔记 #02）——核心工作量
- 改动 284 文件（201A/74M/8D），详见 `board/esp32p4_vela/README.md` 代码地图
- 关键坑：HAL 版本锁定 ≥8d0a898；debug.h 位置差异（68 处替换）；NUTTX_BINARY_DIR 未定义；LD_SCRIPT 多文件；riscv_mtimer 条件编译；**HAL patch 必须固化**（cmake_out 重 clone 丢手工修改，丢过一次）；无 libatomic → 自写 esp32p4_atomic.c；tools/espressif 缺 6 文件；board_app_initialize 缺失 → 新写 appinit
- **8/20 补漏**：`arch/risc-v/Kconfig` 注册块（36 行）第一次提交时漏了——移植 commit 前用 `git status` 对照笔记文件清单逐项核对

### 阶段 3：板卡到货 + 真机识别（8/18）
- 实际为 **ESP32-P4X-C5-Function-EV-Board**（C5-MINI-1 模组）；芯片 rev3.2、flash GD25Q128E 16MB
- console 切 USB Serial/JTAG（ttyACM0）；udev 规则 + dialout 组解决权限

### 阶段 4：烧录攻坚（8/18–19）——当前瓶颈
- **P4 ROM 需要 ram-only-header 格式镜像**（mkimage 旧版不支持，手动 esptool elf2image 生成）
- esptool stub 崩溃（rev3.2 不兼容，5.3.1/4.10 都崩）；ROM 模式 USB 擦写断流；UART0 下载模式无响应
- **重大线索**：ram-only 镜像曾被 ROM 接受并加载，app 早期 crash（load access fault @0x25）——加载链路已通
- 推荐路径：JTAG 烧录（openocd-esp32 已备）；**0x25 crash 反汇编分析应优先于 JTAG**

### 阶段 5：工作区重做 + 提交（8/20）
- **官方 manifest 重做**：`repo init -u <gitee 专属仓> -m contest2026_342_ezdezhandui.xml`（gitee 有镜像且与 GitHub 同步；GitHub 源会挂死）→ `repo sync -c -j8`（两次 TLS 中断重试后成功，旧树保留到新树验证）
- 移植重放发现 Kconfig 遗漏 → 补 → 新树编译通过 + mkimage 自动 ram-only
- 作品仓整理（README/文档/skills/清理模板骨架/manifest linkfile 同步）→ fork + PR #1（CLA 已签，合入权限待组委会）
- **网络攻坚**：github.com:443 直连间歇不通，api.github.com 稳定 → Git Database API push 脚本（/tmp/git-api-push.py）

## 三、已固化资产

| 资产 | 位置 | 说明 |
|---|---|---|
| 移植文件树 | `board/esp32p4_vela/nuttx/` | 284 文件，评审直接可看 |
| 部署/同步脚本 | `board/esp32p4_vela/deploy.sh` `export.sh` | 复现 + 防漂移 |
| 代码地图 | `board/esp32p4_vela/README.md` | 每目录改动目的 |
| 踩坑笔记 | `docs/踩坑笔记_0X_*.md` | 结论先行 + 复现命令 |
| AI 技能集 | `.claude/skills/`（17 个） | 官方 skill + openvela-board-port |
| API push 脚本 | `/tmp/git-api-push.py` | github.com 不通时的推送方案 |

## 四、待优化清单（按优先级）

### 🔴 立即（T1 收尾，已过期）
1. **分析 0x25 crash 现场**（不依赖板子）：`riscv-none-elf-objdump -d cmake_out/.../nuttx` 定位 PC=0x25 附近指令与调用链——若崩溃点很浅（UART/时钟初始化），一次修改就可能点亮串口，比 JTAG 快
2. **JTAG 烧录验证**：动手前先确认 openocd-esp32 对 esp32p4 flash 写入的支持（cfg workarea/stub 配置），避免再次"试了发现不支持"；烧完必须停 OpenOCD 才能看 ttyACM0
3. **git 对齐**：远端 4f959cc9 vs 本地 sha 不同（GitHub 时区规范化，内容等价）——合入后 git fetch 对齐，避免分叉

### 🟡 短期（T1→T2 过渡）
4. **goldfish 回归**：riscv_mtimer 条件编译是全局改动，影响所有 RISC-V 板——重建 goldfish + ai_agent 确认没弄坏（也是演示兜底资产）
5. **HAL patch 可复现性验证**：`rm -rf cmake_out` 全量重构建一次，确认 HAL 重 clone 后 patch 自动 apply（当前从未验证过全流程）
6. **明确 T1 决策点**：8/22 前串口点不亮 → 启动 R1 兜底（goldfish 全功能演示 + 移植材料）
7. **补调试能力**：装 riscv gdb（riscv64-unknown-elf-gdb / gdb-multiarch）+ OpenOCD gdb server——T2 外设驱动调试量大，没有 gdb 很痛苦

### 🟢 中期（T2 开始前定）
8. **外设优先级**：串口点亮 → ETH（联网保底）→ ai_agent+MiMo → 语音（ASR/TTS）→ 摄像头事件主动；LVGL 屏后置（R7 选配可能没到货）
9. **确认屏幕/摄像头配套**（R7 解除）：没随板就调整演示叙事（CLI/串口/语音闭环 + 摄像头降为加分）
10. **AI 日志导出**：contest-log-collector（工作区感应，自动写 logs/<github_login>/）——每次会话结束导出，别到提交前补
11. **移植 PR 上游**（获奖后）：文件树 → 目录直接搬 vendor_espressif/nuttx 结构，PR 到 dev-ai-contest-2026
12. **环境脚本固化**：python2/CMake 坑出现 3 次、pkill 误杀 3 次——写 `tools/env.sh` 一次搞定（PATH/python 软链/串口权限），别每次翻笔记

## 五、反复出现的坑（高频预警）

| 坑 | 出现次数 | 对策 |
|---|---|---|
| python2 抢 CMake Python3 | 3+ | `~/.local/bin/python → python3` 软链已建 |
| `pkill -f` / ps grep 误杀自己 | 3 | `[q]emu` 字符类技巧 |
| cwd 漂移（bash 会话重置） | 反复 | 绝对路径 + `cd <workspace> &&` |
| repo sync TLS 中断 | 每次 | 重试（幂等断点续传），失败仓用不到就忽略 |
| GitHub 直连超时 | 反复 | api.github.com + Git Database API；gitee 镜像；ghfast（不支持 push） |
| CMake find_program 多 NAMES 异常 | 1 | esptool 软链 |
| 移植提交漏文件（Kconfig） | 1 | commit 前对照笔记文件清单逐项核对 |

## 六、真机网络攻坚：eth0 未注册 → 打通（8/30–9/1）

### 问题
ai_agent 需要的 eth0（RJ45）一直未注册：`/proc/net` 空、`ifconfig` 无输出、`ifup eth0` Failed。已确认 NSH→BOARDIOC_INIT→board_app_initialize→esp_bringup→board_emac_init 反汇编链路正确，且 board_emac_init 确实执行（g_hr_timer_initialized=1），但 `g_esp_emac` 结构全 0 → esp_emac_init 在 `dev.ops` 赋值前失败。

### 根因（决定性证据）
- EMAC DMA buffers 由 `heap_caps_aligned_calloc` 从**堆**分配：`(RX20+TX20)×1600B ≈ 48KB`
- 但固件是 **SRAM-only**（512KB PMP 窗口），bss 已占满，堆只剩 **~35KB**（0x4ffb72b4 → 0x4ffc0000）
- `esp_eth_mac_new_esp32` 分配 DMA buffers 失败返回 NULL → `esp_emac_init` 返回 -EIO → eth0 不注册
- 定位方法：OpenOCD 读 `g_esp_emac`（0x4ffac3e0）全 0 vs `g_hr_timer_initialized`=1；对比 defconfig 的 `CONFIG_ESPRESSIF_ETH_NRXDESC/NTXDESC=20` 与堆大小

### 修复
```
CONFIG_ESPRESSIF_ETH_NRXDESC=4
CONFIG_ESPRESSIF_ETH_NTXDESC=4
```
DMA 需求 (4+4)×1600 ≈ 13KB，35KB 堆够用。验证结果：
```
eth0  Link encap:Ethernet HWaddr e8:f6:0a:e3:a6:a5 at RUNNING mtu 576
      inet addr:10.0.0.2 DRaddr:10.0.0.1 Mask:255.255.255.0
```
eth0 RUNNING + 自动获取 IP 10.0.0.2；ps 可见 `esp_timer` + `emac_rx` 任务（EMAC 完全初始化）。

### 经验
1. **DMA buffer 数量要按堆预算算**：ESP32-P4 无 PSRAM 初始化时，大块 DMA 内存只能来自 35KB 堆
2. **OpenOCD RAM flag 证据法**：console 不可用时，靠 `g_hr_timer_initialized`/`g_esp_emac` 内存值反推执行路径（hw bp 在 SMP boot 早期不可靠）
3. **VMware USB 直通抖动**是主要环境障碍：OpenOCD 连接会杀死 USJ console、USB 频繁重新枚举、esptool 时断时连——调试需重试/触发枚举

### 后续（进行中）
- ai_agent 启动触发 assert（printf 后崩溃，pc 停在 `_assert` 死循环）——原因调查中，RAMLOG 诊断固件已备（g_sysbuffer @0x4ffaf03c）
- MiMo key 已编译进 `agent_config.h`（api.xiaomimimo.com / mimo-v2-flash），TLS 闭环验证待 USB 恢复

### ai_agent assert 根因分析（内存不足，已备修复）
- **症状**：ai_agent_main printf 后崩溃，OpenOCD pc 停在 `_assert` 死循环
- **根因**：36KB 堆 vs ai_agent 线程栈需求 ~88KB（agent_loop 32K + voice 16K + outbound 16K + cli 16K + cron 8K）；eth0 打通后 EMAC DMA + 网络栈又占 ~20KB → 堆耗尽 assert
- **修复（已备固件 439648）**：
  - 线程栈裁剪：AI_AGENT 32→16K、VOICE 16→8K、OUTBOUND 16→8K、CLI 16→8K、CRON 8→4K（栈需求 88→44KB）
  - TLS 原始缓冲 2×8K→2×4K（省 8KB bss，堆 36→43.3KB）
  - EMAC DMA NRXDESC/NTXDESC 4→2（DMA 13→6.5KB）
- **诊断固件**：/tmp/nuttx_ramlog_diag.bin（RAMLOG 到 g_sysbuffer @0x4ffaf03c，OpenOCD 可读，USB 恢复后先确认 assert 确为内存）

### ai_agent assert 最终根因 + 修复（9/1 续）
- **最终根因（直接）**：`CONFIG_FS_TMPFS` 未启用 → ai_agent Phase 0 `mount(/data, tmpfs)` 失败 → `/data/agent` 不存在 → config.json 无法创建 → `claw_config_get` 读不到配置（load_json 返回空/异常）
- **定位方法**：printf 分阶段打点（每 phase 打一行）→ 精确定位卡在 `llm_proxy_init` 的第三个 `claw_config_get(LLM_HOST)` → 再在 claw_config_get 内部打点 → 确认卡在 config 读取路径 → `ls /data/agent` = stat failed → 查 mount 只有 /proc → tmpfs 未编译
- **修复**：
  1. `CONFIG_FS_TMPFS=y` + `FS_TMPFS_BLOCKSIZE=512` + 4 个 allocguard/freeguard 配置（defconfig + .config）
  2. `claw_config_get` 加 NULL 保护（load_json 失败返回 NULL 时避免 `cJSON_GetObjectItem(NULL)` 崩溃）
  3. 大量诊断 printf 会导致 console TX 缓冲满阻塞（2000+ 字节打点）——诊断完成后必须清理
- **验证**：打点固件确认 ai_agent 走完 config 层（所有 claw_config_get 正常，root=0x4ffbf398 非 NULL，llm_proxy_init 完成）
- **环境教训**：VMware USB 直通极不稳定（console 读取 40B~2076B 随机、OpenOCD esp_usb_jtag 在固件运行时打不开设备、esptool 时断时连）——硬件验证需稳定 USB 或换调试通道

## 七、USB 直通不稳定调查(9/1)——根因确认 + 修复

### 调查结论
1. **设备无自发断开**:4.5 分钟监控 devnum 稳定(53),零自发断开
2. **断开 = esptool `--before default_reset` 触发**:每次 esptool 硬件复位→设备重新枚举(正常现象)
3. **复位后立即稳定**:esptool 后 3 秒 console 正常(47B NSH)
4. **CP210x 零断开**:排除 VMware USB 直通/总线问题(同总线设备稳定)
5. **OpenOCD 打不开 ≠ USB 问题**:固件运行时 USB-JTAG 的 JTAG 功能被占用,仅 ROM bootloader 可用(用 esptool 串口代替)
6. **供电隐患**:设备请求 500mA,USB 1.1 端口(VMware UHCI 模拟)供电可能不足,建议独立供电

### 修复(已固化 tools/)
- `tools/usb_stable.sh`(原 /tmp/stable_usb.sh):esptool 操作后 `wait_stable`(等 ttyACM0+可打开+3s 缓冲)+ 重试
- `tools/verify_aiagent.sh`:复位→启动 ai_agent→多次读取检测 READY
- 经验:USB 操作后 sleep 3-5s;console 单连接;避免高频 esptool chip_id

### 关联发现:ai_agent 内存不足(独立深层问题)
- 打点定位:ai_agent 入口 `[MEM0] arena=29336 used=22664`——入口就用 22.6KB 堆(arena 仅 28.7KB)
- SIMPLE_BOOT PMP 锁定 [0x4FF40000, 0x4FFC0000),bss 已到 0x4ffb8114,堆只剩 28.7KB
- ai_agent 启动 Phase 0 后剩 5.9KB,无法支撑 Phase 1+ → 非确定性卡住
- **根本方案:PSRAM(32MB)初始化后堆放 PSRAM**(大工程,列入 backlog);短期可减 ai_agent 启动内存/延迟初始化

### ai_agent 内存不足详细定位(9/1 续)
- **根因**:NSH 系统(网络栈+EMAC+NSH)启动后已用堆 23.2KB(`free` 命令:used=23744),堆 arena 仅 28.7KB(SIMPLE_BOOT PMP 锁定,SRAM 512KB 满),剩 5.5KB
- ai_agent 入口 MEM0 used=22664,Phase 0 后 free=5.8KB → message_bus_init 在内存紧张时卡住(定位:MB after syslog 后,msg_bus rc 未返回)
- **已做优化**:ai_agent 主栈 32→16KB(`CONFIG_EXAMPLES_AI_AGENT_VELA_STACKSIZE=16384`);线程栈裁剪;TLS 缓冲 16→8KB;EMAC DMA 2
- **根本方案**:初始化 32MB PSRAM,把堆放 PSRAM(0x48000000)→ 彻底解决;列入 backlog
- 系统内存占用大户:网络栈(IOB/socket)、EMAC、mbedtls——需逐一核算

## 八、PSRAM 初始化调查(9/1)——真机失败,需专门解决

### 目标
32MB PSRAM 初始化,把 ai_agent 运行内存放 PSRAM(根本解决 ai_agent 内存不足)。

### 调查结论
1. **NuttX 已内置 CONFIG_ESPRESSIF_SPIRAM 支持**(esp_start.c 自动调用 esp_psram_chip_init + esp_psram_init + memtest),esp32p4 用 AP HEX(16-line)impl
2. **启用后真机 boot 卡(0 字节)**:SPIRAM=y 时 boot 卡;禁用后正常(28.9KB 堆)
3. **失败点未定**:console(USJ)在 PSRAM 后初始化,PSRAM 日志不可见;OpenOCD 打不开(环境);提前 console 失败
4. **排除项**:
   - 8-line 模式编译错误(esp-hal 与 NuttX 不兼容)→ 回退 16-line
   - MPLL 校准 while 循环:反汇编确认 rtc_clk_mpll_configure 无等待循环(不 hang)
   - mbedtls 冲突(SSL_IN_CONTENT_LEN):sdkconfig.h 硬编码 16384 vs NuttX 4096,#ifndef 缺失 → 已修复(构建期 sed)
5. **怀疑方向**:esp_psram_impl_enable 的 PSRAM 检测(s_check_psram_connected)或 esp_psram_init 的 MMU 映射(esp_mmu_map assert),需专门调试

### 影响
- **PSRAM 是 ai_agent 内存的根本方案**(线程栈 44KB > 堆 28.9KB)
- 当前无 PSRAM 固件(451312)保持可用(基线),但 ai_agent 无法完整运行
- 需专门任务解决:建议 UART0 输出/更好调试环境,对照 ESP-IDF 逐项修复 NuttX esp32p4 PSRAM 支持

---

# 九、烧录根因突破 + 系统启动(9/2–9/3)——决定性修复

> 📌 **主线结果速览（9/4 终点）**：PSRAM 32MB 堆打通（§10.6）→ ai_agent 完整启动稳定运行（§10.8）。
> eth0 联网（§六）+ ai_agent（§十/十一 aux）双线均已闭环。

## 9.1 现象
tabby 连 ttyUSB0(UART0)持续 boot 循环,反复打印:
```
invalid header: 0xc2990ff6
rst:0x7 (HP_WDT) / rst:0x10 (CHIP_LP_WDT_RESET)
```

## 9.2 弯路:先被"没喂狗"带偏
- 用户问"是不是没有喂狗",我一度深入 WDT 分析(WDT 复位是有的,但那是次生现象)
- WDT 复位只是"系统没起来"的结果,不是根因
- **教训:看到复位循环先问"为什么没起来",而不是先查 WDT**;WDT 复位本身是"没起来"的伴随现象

## 9.3 真正根因(决定性证据:USJ 读回 flash)
USJ(ttyACM0)读回 flash 三个偏移各 16B:
```
0x0000: e906022f   ← 固件头(SPI flash 头)
0x2000: f60f99c2   = 0xc2990ff6 ← 固件 offset 0x2000 的数据
0x4000: 82801c41
```
- **ESP32-P4 ROM 从 flash 0x2000 读 boot header(SPI_FAST_FLASH_BOOT),不是 0x0**
- 固件烧在 0x0 时,flash 0x2000 = 固件代码数据(0xc2990ff6 小端 f6 0f 99 c2)→ ROM 读到 invalid header → 死循环
- **修复:固件必须烧 flash 0x2000**

## 9.4 正确烧录命令(已固化)
```
esptool.py --chip esp32p4 -p /dev/ttyACM0 -b 921600 \
  --before default_reset --after hard_reset \
  write_flash --no-compress 0x2000 nuttx.bin
```
- USJ(ttyACM0)烧录稳定 ~1386 kbit/s;`--before default_reset` 自动进下载(等效按 BOOT+EN),`--after hard_reset` 自动复位运行
- UART0(ttyUSB0)115200 慢且 stub 易断,仅用于观察

## 9.5 修复后正常启动链路
```
SPI mode:DIO → load 各段 → entry 0x4ff48736 → Z → *** Booting NuttX *** → NSH
free: 29608 total / 23696 used / 5912 free
eth0: 10.0.0.2 RUNNING
```
- **SIMPLE_BOOT 的 `SHA-256 comparison failed: Calculated <hash> / Expected ffff...` 是正常现象**(ram-only 镜像 digest 未追加),不要当错误处理

## 9.6 弯路复盘(烧录调试)
| 弯路 | 代价 | 正确做法 |
|---|---|---|
| 被"没喂狗"带偏,WDT 分析 | 半天 | 先读回 flash 验证固件位置(决定性),别从症状猜根因 |
| 怀疑 SIMPLE_BOOT SHA 警告 | 少量 | 确认是 ram-only 正常现象 |
| 误判"卡在 E5/nx_start" | 半天 | 实为 USJ console 不可见假象——NuttX console=USBSERIAL 时 ttyACM0 读 0 字节,esp_rom_printf 才走 UART0 |

## 9.7 console 陷阱(已弄清)
- NuttX console = USBSERIAL(ttyACM0)时,boot 后 ttyACM0 读 0 字节不可见;esp_rom_printf(ROM UART)输出到 UART0(ttyUSB0)
- 临时改 `CONFIG_UART0_SERIAL_CONSOLE=y` + 禁 `CONFIG_ESPRESSIF_USBSERIAL` 后 UART0 可见完整 NuttX 输出
- 硬件通道:UART0 = J1 排针 GPIO37/38 → ttyUSB0(CP2102);USJ = ttyACM0(303a:1001)烧录

---

# 十、PSRAM 写 hang 深度调试(9/3–9/4)——最终根因:bootloader PMP 时序缺陷(非硬件 bug)

## 10.1 已确认的事实
- **PSRAM 检测/初始化成功**:esp_psram_chip_init/esp_psram_init ret=0,32MB 映射 0x48000000–0x4a000000
- **CPU 读 PSRAM OK**(cache 读 + 非 cache 0x88000000 读都 OK)
- **CPU 写 PSRAM 卡死(hang)**:任何地址(0x48000000/0x88000000)、任何宽度(1/4/128B)都 hang;无 trap(装了 trap handler 无输出 = 总线等待,非异常)
- **直接 MSPI 写 OK**:psram_ctrlr_ll_common_transaction(SPIMEM3 PERI)写 OK,数据正确
- 差异本质:**CPU/cache 走 SPIMEM2(SYSTEM)写 hang,直接事务走 SPIMEM3(PERI)写 OK**

## 10.2 已排除(40+ 项,每项构建+烧录+测试 ~3-5 分钟)
- SPIRAM 速度 20/80/200M(无关)
- cache 启用(L1/L2 autoload、cache_hal_enable、cache_ll enable)(无关)
- CLIC 中断启用/禁用(无关)
- MMU 权限(mmu_ll 无只读位)(无关)
- PSRAM LDO 供电(esp_ldo_acquire_channel 1.8V)(无关)
- DDR/SDR 模式(PSRAM 必须 DDR;SDR 直接 init 失败)(无关)
- 8-line / hex 数据线(无关)
- wr_splice/rd_splice、split_trans、var_dummy、dqs_loop、outminbytelen、cmd_dis、tx_ddr_msk、addr_4byte、AXI 仲裁(无关)
- cache writeback/invalidate(写回本身不 hang,store hang)
- 烧录地址(已修复)、console(已切 UART0)

## 10.3 对比 NuttX 官方 PSRAM 支持(PR #18754,2026-04 已合并)
- 官方 `esp32p4-function-ev-board:psram_usrheap` defconfig 测试 **PSRAM 写正常**(free 显示 32MB Umem,heap 测试分配 0x480018c0 成功)
- openvela 的 esp_start.c / esp_allocateheap.c / hal_esp32p4.cmake / linker 脚本与 PR 改动逐一核对——**全部都有**
- PR 后无新的 esp32p4 PSRAM 提交
- 配置对齐(SPIRAM=y + MM_REGIONS=2 + MM_KERNEL_HEAP + MEMTEST + 200M)后写仍 hang

## 10.4 核心结论（9/4 修正：推翻"硬件 bug"假设）
- **芯片 rev 判定（决定性）**：`efuse_hal_chip_revision() = major×100 + minor`（esp32p4 特化实现返回 wafer_version_major×100 + wafer_version_minor）→ **302 = v3.2**，与 esptool "revision v3.2" 完全一致
- **此前"302<310 有 bug"是编码误读**：310 不是有效编码（v3.1=301），芯片实际是 **v3.2 ≥ v3.1 修复线**
- ESP32-P4 勘误表 [MSPI-749/750/751 + APM-560](https://documentation.espressif.com/projects/esp-chip-errata/en/latest/esp32p4/03-errata-description/index.html) 明确 **"Fixed in chip revision v3.1"** → 本芯片**硬件已修复，无该 cache/PSRAM bug**
- openvela 的 esp_start.c 已调用官方 PR 全部初始化（esp_mspi_pin_init(634)/esp_mspi_pin_reserve(679)/bootloader_init_mem(681)/esp_mmu_map_init(641)）
- **结论反转：写 hang 是 openvela/NuttX 移植的软件缺陷，不是硬件 bug → 应该能在软件层解决**

## 10.5 判定方法（如何验证"硬件 bug vs 软件 bug"）
1. **芯片 rev 判定**：查 `efuse_hal_chip_revision()` 编码实现（major×100+minor），对照 esptool；再对照勘误表"Fixed in revision"确认硬件是否含该 bug
2. **决定性对照实验（同板跑官方 ESP-IDF）**：同一块板烧 ESP-IDF 官方 PSRAM 测试固件——ESP-IDF 写 OK → 100% 是 NuttX/openvela 移植 bug（软件可修）；ESP-IDF 也 hang → 才可能硬件问题
3. **排除法已做**：PR #18754 改动全部对齐、配置对齐、trap handler 确认非异常（总线等待）、直接 MSPI 写 OK

## 10.6 最终根因与修复（9/4 晚，OpenOCD 决定性诊断）

### 决定性诊断（OpenOCD）
- `halt` → **`Halt cause (7) - PMP Store access fault`**（之前一直误判为"总线 hang"，实际是 PMP 权限拒绝！）
- `mepc=0x4ff4d56a` = `sw a5,0(a4)` 写 PSRAM 指令（riscv_addregion 内）
- 关键：**之前 trap handler 没捕获 + 一直以为是 hang，是因为 PMP fault 是同步异常且死循环 → LP WDT 复位 → UART0 0B 假象**

### 完整根因链
1. `bootloader_init_mem`(bootloader_init 内、**PSRAM init 之前**)在 `CONFIG_BOOTLOADER_REGION_PROTECTION_ENABLE=1` 时调用 `esp_cpu_configure_region_protection()`
2. 此时 `esp_psram_get_heap_size_to_protect()=0`（PSRAM 未初始化）→ PMP 条目 11 配成**空 TOR 区间** `[0x12000000, 0x12000000)`（pmpaddr11=0x12000000 = PSRAM 物理 0x48000000>>2）且 **L 位锁定**
3. PSRAM init 后 region protection 尝试重配 pmpaddr11=0x12800000（32MB），但 **L 锁定 → CSR 写入被硬件拒绝**（固件 + OpenOCD 都写不进）
4. PMP 无 RW 条目覆盖 PSRAM → 默认拒绝写 → **PMP Store access fault** → 死循环

### 修复（2 处）
1. **bootloader_mem.c**：`bootloader_init_mem` 里 `#if 0` 禁用 bootloader 阶段的 region protection（避免 PSRAM init 前错误锁定）：
```c
#if 0 /* openvela: skip bootloader PMP (locks PSRAM write before PSRAM init) */
    esp_cpu_configure_region_protection();
#endif
```
2. **esp_start.c**：`esp_cpu_configure_region_protection()` 在 PSRAM init **后**调用（`CONFIG_ESPRESSIF_REGION_PROTECTION=y`，默认开启，**不要禁用**——之前为"排除 APM-560"禁用它反而加剧了问题）

### 验证（决定性）
- pmpaddr11 = **0x12800000**（正确 RW 覆盖 32MB PSRAM）
- PSRAM 0x48000100 = **0xdeadbeef**（写入成功）
- 系统 boot 到 **idle**（esp_cpu_wait_for_intr），halt cause = **11（ECALL 正常）**，无 PMP fault
- **PSRAM 写 hang 彻底解决，不是硬件 bug、不是 cache 写路径——是 PMP 配置时序缺陷**

### 关键调试经验
- **先看 halt cause**：7=PMP fault vs 11=ECALL，一眼区分"权限拒绝"vs"正常运行"
- **PMP pmpaddr 寄存器存 addr>>2**（0x48000000>>2=0x12000000）
- **esp_usb_jtag 的 OpenOCD 读 CSR 不准、写更无效**（写 pmpaddr11 无效）→ 用固件 `csrr` 读存内存再 OpenOCD 读内存
- **固件里手动 csrw pmpaddr11 被拒（L 锁定）**是定位"PMP 配置失败"的关键证据
- **UART0 被 tabby 占用时 python 读不到**（ttyUSB0 fuser 可见）

## 10.7 最终验证（9/4，tabby MCP 调试）—— PSRAM 32MB 完全可用
- **clean 固件**（bootloader_mem.c 禁用 bootloader PMP + esp_start.c region protection 在 PSRAM init 后 + 移除全部调试打印）正常 boot 到 NSH
- `free` 输出（通过 tabby MCP 发命令 + 读缓冲）：
```
nsh> free
      total       used       free    maxused    maxfree  nused  nfree name
   33576240      23720   33552520      24096   33552440     65      4 Umem
```
- **Umem = PSRAM 32MB 用户堆，完全可用** → ai_agent 内存问题彻底解决
- **调试手段升级：tabby MCP**（tabby-mcp-server 1.6.2，SSE `http://localhost:3001/mcp`）——`get_terminal_buffer` 读 NSH 缓冲、`send_input` 发命令、`close_tab` 关串口会话释放端口（ttyACM0 被 tabby 占用时 esptool 烧录失败，close_tab 解决）、`get_session_list` 定位会话
  - 客户端脚本：`tools/tabby_mcp_monitor.py`（含 Mcp-Session-Id header 处理，否则 400 "Server not initialized"）
  - **ttyUSB0/NSH 被 tabby 占用时 python 直读 0B，但 tabby MCP 可正常读取/交互**（零冲突）

## 10.8 ai_agent 完整启动成功（9/4，tabby MCP + OpenOCD 联调）—— 主线闭环

### 10.8.1 现象
PSRAM 32MB heap 就绪后，ai_agent 能启动但卡死在 `llm_router_init` 之后的 `tool_registry_init`
（"Router initialized" 后无输出），随后反复 **PMP Load access fault**，整机看似"卡死"。

### 10.8.2 Root cause（OpenOCD 断点/栈分析确认）
1. ai_agent 作为 NSH 前台 builtin 命令，实际在 **nsh_main 任务栈（仅 1984B）** 上执行
   （task_spawn 后 proxy 先行、preemption disabled 期间深 init 链跑在当前任务栈）
2. ai_agent init 的深调用 + cJSON/mbedTLS 栈需求远超 1984B → **nsh_main 栈溢出**
3. 栈溢出写穿覆盖 TLS 区 → **TLS[0]（`tl_task`）被写为 0**
4. `getumask` / `fdopen`（经 `lib_get_streams`：`g_readytorun[112]→[0]→+296`）读空指针偏移
   → **PMP Load access fault**（mtval=0x128），异常进 up_mdelay 死循环 → 整机"卡死"

**定位证据链**（为什么是栈问题而非 PMP/cache）：
- OpenOCD 断点命中 ai_agent_main：`sp=0x4ffbb9c0` 挤在 nsh_main 栈顶（0x4ffbb410）附近，
  `g_readytorun` 仍指向 nsh_main（pid=2）→ 前台复用 nsh_main 栈，而非独立 16KB 任务栈
- fault 现场反复出现 `TLS[0]=0`（复位后正确值 0x4ffbb058）→ TLS 头被覆盖
- `sp=0x4ffb0898` 低于 nsh_main 栈底 ~744B → 栈已溢出写穿

### 10.8.3 修复
```
CONFIG_DEFAULT_TASK_STACKSIZE=16384   # defconfig：nsh_main 栈 1984B → 16320B
```
- 安全网（保留，防同类 TLS 损坏）：`lib_umask.c` 的 `getumask()` 加 `info != NULL` 判断

### 10.8.4 验证（决定性）
- `ps`：nsh_main STACK 从 `0001984` → `0016320`
- ai_agent 完整输出 **P0→P5 全 rc=0**：
  P0 timezone/storage/heap → P1 config_store/message_bus/memory_store/session_mgr →
  P2 http_proxy → P3 llm_proxy/llm_router/**tool_registry(36 tools)**/**tool_guard**/**skill_loader(10 skills)**/
  agent_loop/cron/heartbeat/voice_channel → P4 nsh_commands_init → P5 outbound dispatch/cron+heartbeat/network_watch
- 系统自由运行 **30s+ 无 PMP fault**（OpenOCD Halt cause **11 = ECALL 正常**）
- ai_agent 线程栈分配在 **PSRAM**（当前任务 0x4800fc90，heartbeat 线程 pid6 @0x4800caa0）→ PSRAM 32MB 完整可用

### 10.8.5 运行模式说明与遗留
- **P6 CLI**：ai_agent 前台由 NSH spawn 后 main 线程进入 `while(!g_shutdown_requested) sleep(1)`
  长循环，NSH waitpid 阻塞属**设计行为**（ai_agent 是长服务）；agent_cli 线程从 stdin 读命令
  （提示符 `vela> `），演示环境建议以系统服务/自启方式接入而非 NSH 前台
- **遗留**：`ai_agent &`（NSH 后台运行）会触发 ROM 区（0x4fc1b674，libc 内空指针）PMP fault，
  属次要场景（前台演示不受影响），未深挖

### 10.8.6 本节调试方法沉淀
| 手段 | 用途 |
|---|---|
| OpenOCD 断点（hw bp）命中函数入口 | 读 sp/TLS 链，判定"谁在跑、跑在谁的栈上" |
| TLS 链读取 `g_readytorun[112]→[0]→+296` | 定位 tl_task=0 的源头（TLS 覆盖） |
| sp 与 stack_alloc_ptr 对比 | 判定栈溢出（sp < 栈底即溢出） |
| Halt cause 区分 | 11=ECALL 正常 vs 5/7=PMP Load/Store fault |
| tabby MCP（ttyUSB0 被占用时） | `send_input`/`get_terminal_buffer` 零冲突交互 |

---

# 十一、构建/配置弯路汇总(高频,必须固化)

| 坑 | 症状 | 修复/对策 |
|---|---|---|
| **march 丢失** | 手动 `cmake .` 后 `-march=rv32imac` 丢了 `_zicsr_zifencei` → csrr 报错 | `sed -i 's/-march=rv32imac /-march=rv32imac_zicsr_zifencei /g' build.ninja`(每次 reconfigure 后重做) |
| **config.h 过期** | 删 .config 后 config.h 未重新生成,SPIRAM 宏缺失 | `rm -f .config .config.prev include/nuttx/config.h && cmake .`(删 config.h 强制重生成) |
| **sdkconfig.h 与 config.h 冲突** | CONFIG_MBEDTLS_SSL_IN_CONTENT_LEN 16384 vs 4096 redefined | sdkconfig.h 加 `#ifndef` 保护 |
| **SPIRAM heap 不生效** | free 显示 21KB 无 PSRAM | 需 `CONFIG_MM_REGIONS=2`(riscv_addregion 才编译)+ include `<nuttx/kmalloc.h>`(kumm_addregion) |
| **8-line 模式编译错误** | flash_ops.c 缺 esp_opiflash 函数 | 回退 16-line(AP HEX) |
| **MM_KERNEL_HEAP 不适用** | 启用后 boot 0 字节 | openvela 是 flat build,PSRAM 走 kumm_addregion 而非 kernel/user 堆分离 |
| **sed 误删代码** | 清理调试标记误删 set_wr_cmd / 残留参数行 → 编译错 | 用 edit 精准删除,别用 sed 大段匹配 |

---

# 十二、改进清单更新(9/4)

> ⚠️ 本清单为 **9/4 当天的历史快照**。其中多数项随后已闭环（见 §14.1）：
> **#2 PSRAM 决策点** → 已解决（rev3.2 无硬件 bug，PMP 时序修复后 32MB 可用，见 §10.6）；
> **#3 eth0+ai_agent 基线** → ai_agent 已完整运行（§10.8）；**#7 PSRAM 内存优化** → 不再需要。

### 🔴 立即（当时）
1. **清理调试标记**:esp_start.c(D1-D7/E1-E5/SP1-SP4/REV/CACHE-EN)、nx_start.c(N/H/up_putc 123456)、esp_allocateheap 测试代码;决定 console 恢复 USBSERIAL 或保留 UART0(defconfig 行 79/155 需恢复或确认)
2. ~~**PSRAM 决策点**~~ ✅ 已解决:rev3.2 无硬件 bug → PMP 时序修复 → 32MB PSRAM heap 可用(§10.6/10.7)
3. ~~**eth0 + ai_agent 基线回归**~~ ✅ ai_agent 完整运行验证通过(§10.8),禁 SPIRAM 基线已可退役

### 🟡 短期
4. **验证后清理 MiMo key 再公开提交**(agent_config.h 严禁进公开仓;fork push 到 dev-ai-contest-2026)
5. **USJ console 不工作排查**(ttyACM0 boot 后 0 字节,独立于 PSRAM 的问题)
6. **HAL 版本对齐**:openvela esp-hal-3rdparty 与 NuttX master 克隆版本差异核对(esp_psram_impl_ap_hex.c 缺 BURST_WRITE 定义,标记版本旧)

### 🟢 中期(当时)
7. ~~**PSRAM 若不可用**:ai_agent 内存优化~~ ✅ 不再需要——PSRAM 32MB 已可用
8. **goldfish 回归 + 演示兜底资产**(riscv_mtimer 全局改动确认没弄坏)

---

# 十三、本轮最大教训(写给自己)

1. **先验证物理事实,再猜根因**:烧录问题 3 秒读回 flash 就能定(0x2000 vs 0x0),却先做了半天 WDT 分析。凡"复位循环/启动失败",第一步永远是读回 flash / 抓完整 boot 日志,确认固件到底在不在正确位置。
2. **console 通道决定你能看到什么**:USJ console 不可见 ≠ 系统没启动。改 console 到 UART0(esp_rom_printf)前,不要下"卡死"结论。
3. **盲试配置是最贵的调试**:PSRAM 写 hang 40+ 次构建+烧录(每次 3-5 分钟),很多是重复的寄存器排列组合。应更早:(a) 用 trap handler 区分 hang vs trap(一次就排除异常);(b) 对比官方 PR #18754 的已验证配置(早该查);(c) 一次验证"官方配置能否跑"而非逐一改寄存器。
4. **决定性问题优先**:验证"是否硬件 bug"应靠"官方同样代码在别的板/rev 能否写 PSRAM",而不是靠穷举我的配置。
5. **保留基线**:每轮调试前确认有一个可跑固件(当前:禁 SPIRAM 基线),避免改坏后无退路。
6. **sed 大段删除会误伤**:清理调试代码用 edit 精确操作,别用 sed 匹配删除。

---

# 十四、主线闭环后的当前状态与下一步（9/4）

## 14.1 已达成（主线全部闭环）
| 里程碑 | 状态 |
|---|---|
| PSRAM 32MB heap 可用（PMP 时序修复） | ✅ `free`：Umem total=33576240 |
| eth0 RJ45 联网（DMA 裁剪） | ✅ 10.0.0.2 RUNNING |
| ai_agent 完整启动 P0→P5 | ✅ 36 tools / 10 skills 注册 |
| ai_agent 稳定运行（无 PMP fault） | ✅ 30s+ Halt cause 11 |
| 线程栈落 PSRAM | ✅ 0x4800fc90 等 |

## 14.2 下一步（按优先级）

> 更新（9/7）：1、2、6、9 已在 §十六阶段闭环；4、5、7、8 仍待办（详见 §16.6 与下方勾选）。

### 🔴 立即
1. ✅ **清理剩余调试标记**（9/4 已完成，§14.3）
2. ✅ **保存修复为正式改动**（文件树收拢进作品仓，§16）
3. **验证 ai_agent 实际功能链路**：网络连通后测试真实 LLM 请求
   （MiMo key 走 agent_secrets.h 注入，api.xiaomimimo.com）——TLS 闭环是演示核心

### 🟡 短期
4. **ai_agent 接入方式决策**：NSH 前台（当前）vs init 自启/服务化——演示要"上电即 AI 可用"
5. **`ai_agent &` ROM fault 调查**（次要）：后台运行场景若演示需要再深挖
6. ✅ **清理 MiMo key 再公开提交**（9/7 完成：agent_config.h 空 key + agent_secrets.h 注入；⚠️ 但 4d7f318 历史已泄露 key，须轮换，见 §16.5）
7. **goldfish 回归**（riscv_mtimer 全局改动确认没弄坏，演示兜底资产）

### 🟢 中期（9/10 演示冻结前）
8. **演示脚本/叙事固化**：语音闭环（ASR/TTS）或 CLI 对话 + 摄像头事件（若到货）
9. ✅ **AI 日志导出**：contest-log-collector 已装，logs/ez-xu/ 持续导出

---

# 十五、时间线与文档修订历史

| 日期 | 里程碑 | 对应章节 |
|---|---|---|
| 8/6–8/20 | 环境搭建 → 移植 → 烧录攻坚 | §二 |
| 8/30–9/1 | eth0 打通 + ai_agent 内存不足定位 | §六 |
| 9/1 | USB 直通调查 + PSRAM 调查 | §七、§八 |
| 9/2–9/3 | 烧录根因突破（flash 0x2000） | §九 |
| 9/3–9/4 | PSRAM 写 hang 深度调试 → PMP 根因 | §十 |
| 9/4 | PSRAM 32MB heap 验证 + ai_agent 完整启动 | §10.7、§10.8 |
| 9/5–9/7 | 三仓改动收拢（文件树+293 copyfile）+ 分支整理干净线性 + PR #7 + README 四步复现 | §十六 |
| 9/7 | 发现并修复 API key 泄露（agent_secrets.h 机制） | §16.5 |
| **9/10** | **演示冻结** | — |
| **9/18** | **提交** | — |
| 9/20 | 截止 | — |

### 14.3 代码清理与固化（9/4 晚，同会话）
- **调试标记已清理**：
  - `nx_start.c`（H1-H13/N1-N8/123456）→ `git checkout` 还原（纯调试，22 行）
  - `riscv_initialize.c`（up_putc '1'-'6'）→ `git checkout` 还原（纯调试）
  - `esp_start.c`（D1-D7/E1-E5/P1/P2/Z 共 15 行 esp_rom_printf + `#if 1 /* KEEP */` 结构）→ 精准删除
  - `esp_allocateheap.c` 多余 cache_ll/cache_hal include → 移除，仅留 `nuttx/kmalloc.h`
- **保留的正式修复（4 文件）**：
  1. `esp_start.c`：region protection 移到 PSRAM init 后 + cache enable（PSRAM PMP 核心修复）
  2. `defconfig`：SPIRAM=y / MM_REGIONS=2 / DEFAULT_TASK_STACKSIZE=16384 / FS_TMPFS=y /
     ETH NRXDESC=NTXDESC=2 / AI_AGENT_STACKSIZE=16384 / UART0 console
  3. `lib_umask.c`：getumask NULL 防御
  4. `esp_allocateheap.c`：kmalloc.h include（kumm_addregion 原型）
- **验证**：清理后固件 458272B（-656B），正常 boot + ai_agent 完整跑通 P0→P5，
  OpenOCD Halt cause 11 正常，线程跑 PSRAM
- ⚠️ **符号地址偏移教训**：清理打印后固件重排，`g_readytorun` 等符号地址会变
  （0x4ff9df48 → 0x4ff9dd48）——每次重建后用 `nm` 重新查符号地址，勿用旧地址读内存

---

# 十六、三仓改动收拢 + 分支整理 + PR 提交（9/5–9/7）

> 📌 **本阶段核心**：把工作区 nuttx/apps/ai_agent 的全部改动以「文件树 + repo manifest copyfile」收进作品仓，
> 整理成干净线性历史，形成可评审复现的交付（PR #7）。

## 16.1 交付形态决策
- **问题**：nuttx/apps 仓无法直接合入（比赛作品仓是独立仓库），ai_agent 也尚未提交
- **方案**：作品仓 `board/esp32p4_vela/` 保存全部改动文件（文件树），
  `contest2026_342_ezdezhandui.xml` 用 **293 条 `<copyfile>`** 在 `repo sync` 时自动映射到工作区对应路径
  - nuttx 281 文件 + 8 删除（`.deleted-files` 记录）
  - apps 3 文件（mbedtls 配置）
  - ai_agent 9 文件（条件编译裁剪 + stubs）
- **linkfile 不可行**（跨仓软链语义不符）→ 逐个 copyfile 映射

## 16.2 同步脚本升级（export.sh / deploy.sh）
- `export.sh` 双模式：
  - **committed 仓**（nuttx/apps）= `git diff BASE..HEAD --name-status`
  - **worktree 仓**（ai_agent 无 commit）= `git status --porcelain`（注意 ` M` 前导空格须去空格再取首字母）
- `deploy.sh` 三仓部署：nuttx/apps/ai_agent + 按 `.deleted-files` 删除（幂等 rsync）
- `lock-revision.sh`：manifest self-ref revision 一键锁定到最新 HEAD（双份同步：作品仓 + .repo/manifests）
- **关键坑**：self-ref project revision 用分支名报 "not found"（repo 从 manifest 推断 remote 为上游 org），
  **必须用 SHA 锁定**才可 local-only sync

## 16.3 repo sync 复现验证（README 四步全流程）
| 步骤 | 命令 | 结果 |
|---|---|---|
| 1. init | `repo init -u <url> -b dev-ai-contest-2026 -m contest2026_342_ezdezhandui.xml` | ✅ 复用现有 checkout |
| 2. sync | `repo sync -c -j8` | ✅ copyfile 自动落位 293 条 |
| 3. deploy | `./deploy.sh <工作区根>` | ✅ 三仓部署 + 8 删除生效 |
| 4. build | `./build.sh configs/nsh/ --cmake -j8` | ✅ 26s 成功 nuttx.bin 432KB |

- **repo sync 分叉坑**：manifest 仓 default 分支本地有 commit 领先 origin → repo 想 rebase → UpdateManifestError
  → 修复：`repo sync -l --no-manifest-update <project>`（跳过 manifest 仓更新）
- repo sync 后作品仓 checkout 到 manifest 锁定 revision（detached），本地分支不受影响

## 16.4 分支整理（干净线性历史）
- **问题**：历史中存在重复提交（同一内容不同邮箱提交，如 323e628 == 98f1a90 tree 相同）
- **处理**：不用 merge（会产生 merge commit + 重复历史），重置到 fix-clic 干净线性链
  - dev-ai-contest-2026 = 上游基线 3c06c95 之后 **17 个唯一提交**（纯线性，0 merge）
  - 脚本验证全链 26 提交 tree **0 重复**
  - 本地 + fork 只保留 dev-ai-contest-2026 单一分支
- **PR 承接**：PR #6（head=fix-clic-interrupt）关闭 → 新建 **PR #7**（head=dev-ai-contest-2026）

## 16.5 ⚠️ API key 泄露事件（9/7 发现，已修复）
- **事故**：ai_agent 收拢时把真实 MiMo API key 从工作区 `agent_config.h` 带进了作品仓
  （`board/esp32p4_vela/ai_agent/include/agent_config.h`），随 commit `4d7f318` push 到公开 PR #7
- **根因**：收拢时未执行 §14.2 #6 的"清理 key 再提交"检查；上游设计本用 `agent_secrets.h`
  （`.gitignore` 忽略 `*secrets*`）注入真实 key，`agent_config.h` 默认 key 应为空
- **修复**：
  1. `agent_config.h` 恢复空 key（`#define AGENT_SECRET_API_KEY ""`）
  2. 本地新建 `include/agent_secrets.h`（被 .gitignore 忽略）注入真实 key，构建不受影响
  3. 提交净化 commit（a43839d）→ 同步 .repo/manifests → push fork
- **⚠️ 遗留**：key 已在公开 GitHub 历史（4d7f318）泄露，**GitHub 历史不可抹除 → 必须轮换该 MiMo key**
- **教训**：凡是含"secret/key/token"字样的文件，提交前必须 grep 扫描；机密一律走被忽略的本地文件

## 16.6 未完成项（本节结束时）
- goldfish 回归（riscv_mtimer 全局改动影响所有 RISC-V 板，未验证）
- ai_agent 真实 LLM 请求验证（TLS 闭环，MiMo key 需先轮换）
- `ai_agent &` ROM fault 调查（次要）
- 演示脚本/叙事固化（语音闭环 / CLI 对话 / 摄像头事件）
